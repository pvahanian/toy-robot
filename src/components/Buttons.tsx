import React from "react";
//style buttons
import { ButtonWrapper } from "../App.styles";

const Buttons = () => {
  return (
    <div>
      <ButtonWrapper>
        <button>Go</button>
        <button>Reset</button>
      </ButtonWrapper>
    </div>
  );
};

export default Buttons;
